org.gradle.jvmargs=-Xmx1G

# 各 *_version はコンパイル時のAPI参照用で、実行時の対応はローダーの導入版次第（Fabricの下限は fabric.mod.json の depends）
loader_version=0.19.2
forgespi_version=8.0.0
fancymodloader_version=11.0.13

mod_version=0.1.0
maven_group=vms
archives_base_name=version-mod-sorter
